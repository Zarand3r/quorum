# Pinned dependencies for the vivarium pip hub (vivarium_deps).
# Route A (M0-M2) is plain numpy — NO autograd, NO torch (see DECISIONS.md D9).
# Pins mirror the sibling thermolife lock for a consistent pytest stack.
colorama==0.4.6 ; sys_platform == 'win32'
iniconfig==2.3.0
numpy==2.4.6
packaging==26.2
pluggy==1.6.0
pygments==2.20.0
pytest==9.1.0
pyyaml==6.0.2
