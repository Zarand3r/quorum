[pytest]
python_files = test_*.py
addopts = -q --tb=short --strict-markers
markers =
    invariant: P1-P9 gate (locality, one-clock, measure-not-rewarded, determinism, ...)
    slow: long-horizon runs (5k+ ticks)
