"""HTTP visualization for the toy v1 Schelling sim.

Serves a single-page app that runs the mock sim on demand and animates the
grid + same-color-fraction trace client-side. Bound behind Tailscale
Funnel for a public HTTPS URL; see the module docstring in ``server.py``.
"""
