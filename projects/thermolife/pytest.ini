[pytest]
python_files = test_*.py
addopts = -q --tb=short --strict-markers
markers =
    invariant: PLAN.md I1-I15 gate (conservation, non-negativity, no-free-energy, determinism, ...)
    slow: long-horizon runs (10k+ ticks)
