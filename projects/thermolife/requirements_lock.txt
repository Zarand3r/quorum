# Pinned dependencies for the thermolife pip hub (thermolife_deps).
# Slice 0 is numpy + pyyaml + pytest only — no torch, no matplotlib, no web
# framework (the server is stdlib http.server; the viewer is a client canvas).
# Pins mirror the sibling quorum/experiments lock for a consistent pytest stack.
colorama==0.4.6 ; sys_platform == 'win32'
iniconfig==2.3.0
numpy==2.4.6
packaging==26.2
pluggy==1.6.0
pygments==2.20.0
pytest==9.1.0
pyyaml==6.0.2
