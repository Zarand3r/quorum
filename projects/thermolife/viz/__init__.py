"""thermolife.viz — read-only rendering (PLAN.md §5, §12).

Interface renderer must call the SAME code path as the binding kernel
(groundedness, I11): a hand that morphs on screen <=> its interaction kernel
changed. R(phi) = r0 + a1 cos + b1 sin + a2 cos2 + b2 sin2 from the actual
ligand/receptor code.

Planned modules (PLAN.md §12): world_renderer, interface_shapes,
binding_animation, trajectory_plots. No implementation yet.
"""
